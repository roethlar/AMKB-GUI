# cl-14: Queued media render can mutate a workspace that left Source

**Severity**: MEDIUM — an ordinary tool switch can make delayed media work stop
unrelated Board playback without an error or user action.
**Status**: In progress
**Branch**: —
**Commit**: pending

## Evidence

- `am_configurator/web/app.js:3238-3260` validates only draft identity and
  revision before dispatching `SEQUENCE_RENDER_STARTED`.
- `am_configurator/web/lighting_workspace.js:807-825` handles that event by
  stopping playback and advancing the preview request epoch unconditionally.
- `am_configurator/web/app.js:3275-3290` checks route, tool, destination, and
  media ownership only after preview-session acquisition; its discard changes
  the media draft but does not terminate the workspace request it already
  started.
- `am_configurator/web/lighting_composer.js:122-171` permits one running task
  plus one latest queued task. The queued task can begin well after the user
  leaves Source.
- `am_configurator/web/app.js:2563-2568` changes studio tools without
  cancelling queued media work.

The reviewed range was independently traced after intake. A queued task retains
the same catalog ID and draft revision across `TOOL_SELECTED`, so its initial
guard passes even though Source no longer owns the workspace.

## Predicted observable failure

Queue a second framing render behind a slow full GIF render, switch to Paint or
Effects, and start Board playback. When the queued render begins, playback
stops and the play button returns to Play. The discarded request can also leave
the current preview marked updating with no matching terminal completion.

## What

The scheduler correctly prevents overlapping full renders, but task ownership
is checked after the task has already mutated the shared workspace. Tool
transitions do not cancel pending media work.

## Approach

Reject a queued request against exact route, tool, destination, and media
identity before dispatching any render-start event. Cancel pending media
scheduler work whenever the user leaves Source; an already-running request
remains harmless because its captured context cannot publish after the
transition. If an exact current request is discarded after it starts, terminate
that request through a reducer-owned event rather than leaving preview state
updating.

## Files changed

Frozen repair scope:

- `am_configurator/web/app.js` — capture queued-request ownership, reject it
  before render-start mutation, and cancel pending work when Source loses the
  workspace;
- `tests/web/lighting_shell.test.js` — enforce the ownership-check ordering and
  centralized cancellation wiring;
- `am_configurator/media_framing_audit.py` — exercise the delayed-render,
  queued-render, tool-switch, and unrelated-playback sequence in the native
  app; and
- `tests/test_media_framing_audit.py` — guard the native audit contract.

Only this finding record, the shared review index/outcome, and current state may
join those files when the repair closes. `lighting_workspace.js` does not need
a new event because its existing tool/route transitions already reset preview
state and stale running responses are reducer-rejected.

## Guard proof

`tests/web/lighting_shell.test.js` —
`queued media renders surrender Source ownership before shared preview
mutation` requires queued work to capture the exact workspace/media revision,
requires pending work to be cancelled before transition intents execute, and
requires the ownership rejection to occur before `SEQUENCE_RENDER_STARTED`.

`am_configurator/media_framing_audit.py` — the native WebView2 audit holds one
full render in flight, queues a newer framing revision, switches to Paint,
starts the independent document Board playback, releases the stale render, and
requires playback and the Paint preview to remain unchanged. It then returns to
Source and proves the current media revision can still render and Apply.

Proof:

- Focused green: 63 shell/composer/workspace browser tests and all 15
  media-framing audit tests passed.
- Native green: GIF, PNG, and BMP passed at 1000×680 and 1280×800 with the
  delayed-render ownership check, zero console errors, and zero layout
  findings.
- Mutation proof: removing only the `app.js` ownership capture, pre-mutation
  rejection, and Source-exit cancellation made the focused browser guard fail
  and made the native audit exit 1 with
  `queued_render_stopped_playback`. Restoring those lines returned both guards
  green; no mutation marker remains.
- Full gate: 694 Python tests passed with 5 skips; all 158 browser tests passed;
  Python compilation, all seven JavaScript syntax checks, and both source and
  wheel builds completed successfully. Machine-local evidence uses
  `cl14-final-full-gate-20260802-053517`,
  `cl14-native-audit-final-20260802-053448`, and
  `cl14-mutation-red-20260802-053049`.

## Coder dispute (if any)

None. The task mutates workspace state before evaluating the predicate that
would reject it.

## Known gaps

The selected-frame/full-sequence two-tier scheduler is separately tracked by
`cl-16`; this repair must remain valid after that scheduler is wired.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`e97b40280a494ff5446fb2954fe01ed84f565924..7052212445c269752a094217b1ab4813741b2ef7`;
claude-cli 2.1.220; capability_ok=true; verdict=findings; exit 0; no stderr;
2026-08-02T09:18:12Z.

- The schema-enforced first substantive result carried exact pinned SHAs and
  three findings. It was persisted and used as returned without retry,
  re-emission, replacement, reformatting, or resubmission.
- The launch recorded the known hook rewrite of one git command to `rtk` and
  one denied Grep against a temporary file. Allowed repository reads and git
  inspection completed; the environment quirk does not invalidate the result.
