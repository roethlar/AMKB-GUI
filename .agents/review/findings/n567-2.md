# n567-2: Shared validation rejects every native Neon keymap

**Severity**: HIGH — 2026-07-26 `openreview codex` over
`6c396c9..d17c2ca` (plan tasks N5, N6, N7).
**Status**: Open
**Branch**: `neon-80-support`
**Commit**: (not started)

## Evidence

am_configurator/server.py:457 - validation requires exactly 200 keycodes per layer although NeonTransport reads 90; line 548 also invokes the serial writer planner.

## Predicted observable failure

The four 90-key layers read from a Neon fail /api/config/validate, preventing the write dialog from opening; serial planning also cannot authoritatively validate axial/head HID packets.

## What

See the pass summary in `.agents/review/index.md`: the protocol modules are
sound in isolation and the seams between them and the existing application were
not built. This finding is one of those seams.

## Approach

Not started. The reviewer's suggestion: Make validation family-aware through FamilySpec or the selected driver, require the Neon's exact layer geometry, and use the Neon packet planner instead of writer.plan.

## Files changed

Not started.

## Guard proof

Not started.

## Coder dispute (if any)

None.

## Known gaps

None recorded yet.

## Reviewer comments

`Reviewer: codex / (codex-configured default)` — verdict `findings`.
