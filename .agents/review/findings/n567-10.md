# n567-10: Neon keymap layout and assignment filtering were not integrated

**Severity**: MEDIUM — 2026-07-26 `openreview codex` over
`6c396c9..d17c2ca` (plan tasks N5, N6, N7).
**Status**: Open
**Branch**: `neon-80-support`
**Commit**: (not started)

## Evidence

am_configurator/web/app.js:604 - activeLayout handles Relic and AFA then falls back to a generic matrix; the shared assignment palette also remains unfiltered for Neon.

## Predicted observable failure

A read Neon keymap is displayed in an unrelated 25-column layout, while media and Angry Miao assignments that the backend rejects remain selectable instead of failing at assignment time.

## What

See the pass summary in `.agents/review/index.md`: the protocol modules are
sound in isolation and the seams between them and the existing application were
not built. This finding is one of those seams.

## Approach

Not started. The reviewer's suggestion: Publish the validated Vial definition's physical layout to the browser and use a Neon representability projection to filter palette entries and reject unsupported raw assignments immediately.

## Files changed

Not started.

## Guard proof

Not started.

## Coder dispute (if any)

None.

## Known gaps

None recorded yet.

## Reviewer comments

`Reviewer: codex / (codex-configured default)` — verdict `findings`.
