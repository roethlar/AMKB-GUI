# cl-22: Source-text guard pins a dead clone block

**Severity**: LOW — removing unused code during a correct repair fails the
recorded browser gate even though runtime behavior improves.
**Status**: Verified
**Branch**: —
**Commit**: `3b2d26fb48094bf8b804a0449cb968edc2b4b7d9`

## Evidence

- `am_configurator/web/app.js:4563-4565` clones the page, applies imported
  lighting to the clone, and assigns lightness, but never reads or validates
  that candidate before separately mutating the live page.
- `tests/web/lighting_shell.test.js:899-946` requires the unused candidate
  statement by regular-expression source matching.

## Predicted observable failure

Deleting the unused candidate block as correct cleanup causes
`node --test tests/web/lighting_shell.test.js` to fail even though Apply retains
one Undo checkpoint and the same user-visible result. The verification gate
therefore rejects a strictly correct cleanup.

## What

A source-text assertion treats an implementation detail with no observable
effect as required behavior and makes the dead block load-bearing.

## Approach

Removed the unused candidate clone/apply/lightness block from
`applyImportedLighting()`. The existing boundary test now rejects that dead
path explicitly while retaining its executable contract checks for one live
mutation, no extra Undo call, no implicit Library request, and mutation-free
Save and Close paths.

## Files changed

- `am_configurator/web/app.js` — remove the unused candidate clone, apply, and
  lightness assignment.
- `tests/web/lighting_shell.test.js` — reject reintroduction of that dead path
  while retaining the real Apply boundary assertions.
- `.agents/review/findings/cl-22.md`, `.agents/review/index.md`,
  `.agents/state.md`, and the redesign plan — repair state and proof.

## Guard proof

- `tests/web/lighting_shell.test.js` — `imported lighting review keeps Apply,
  Library save, and Close boundaries separate` rejects any candidate clone,
  candidate apply, or candidate lightness assignment in the import Apply path.
- Red: restoring only the three reviewed dead lines made exactly that focused
  test fail on its negative assertion; the other 41 shell tests passed.
- Green: removing the lines passes 42 focused shell tests and the complete
  720-Python/179-browser/compile/syntax/build gate with 5 Python skips.

## Coder dispute (if any)

None. The cited statement has no consumer and the test requires it literally.

## Known gaps

The frozen-array runtime failure is closed separately as `cl-21`; this repair
changes only the dead block and its vacuous positive guard. Per the owner's
2026-08-02 review-economy ruling, this minor cleanup receives no external model
review; its mutation proof, full gate, and exact-head CI are authoritative.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`b927a97d4a96c9b456ca5964f4b93181a48a023d..845f716fdc80741f38ec2161e49e7b775114fe3c`;
claude-cli 2.1.220; capability_ok=true; verdict=findings; exit 0; no stderr;
2026-08-02T15:46:55Z.

- The schema-enforced first substantive result carried the exact pinned SHAs
  and two findings. It was persisted and used as returned without retry,
  re-emission, replacement, reformatting, or resubmission.
- The reviewer recovered from two non-terminal denied inspection spellings
  through allowed repository reads, git inspection, and Node execution. Its
  transcript model key was `claude-opus-5`; immutable result SHA-256 is
  `27FB81F2BBF70D09260B1920E36B58B366787EB0679C954281DC166D9D96E51F`.
- This finding was independently confirmed from the landed code and guard
  before admission.

Closed locally under the owner's 2026-08-02 review-economy ruling; no external
per-finding review was run.

- Reintroducing only the three removed dead lines made exactly the named shell
  guard fail while the other 41 focused tests passed; restoring the repair
  returned all 42 focused tests green.
- The complete local gate passed 720 Python tests with 5 skips, 179 browser
  tests, compile/syntax checks, and both package builds.
- Exact-head CI run `30756144641` and Desktop installers run `30756144701`
  passed every job for repair commit
  `3b2d26fb48094bf8b804a0449cb968edc2b4b7d9`.
