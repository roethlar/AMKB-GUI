# n567-8: Successful Neon writes crash before persistence

**Severity**: HIGH — 2026-07-26 `openreview codex` over
`6c396c9..d17c2ca` (plan tasks N5, N6, N7).
**Status**: Open
**Branch**: `neon-80-support`
**Commit**: (not started)

## Evidence

am_configurator/server.py:2148 - completion reads after.version, but HidDeviceInfo has no version field, only protocol_version.

## Predicted observable failure

After hardware mutation and successful verification, the endpoint raises AttributeError and returns HTTP 500 without saving the current configuration or snapshot, encouraging an unsafe repeat write.

## What

See the pass summary in `.agents/review/index.md`: the protocol modules are
sound in isolation and the seams between them and the existing application were
not built. This finding is one of those seams.

## Approach

Not started. The reviewer's suggestion: Define and enforce a common device-info contract with version present for every transport, using null or a validated firmware value for Neon.

## Files changed

Not started.

## Guard proof

Not started.

## Coder dispute (if any)

None.

## Known gaps

None recorded yet.

## Reviewer comments

`Reviewer: codex / (codex-configured default)` — verdict `findings`.
