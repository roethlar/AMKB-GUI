# cl-11: Sessionless renders consume and depend on the preview-session LRU

**Severity**: MEDIUM — ordinary full renders can evict an unrelated live preview
session or be cancelled by cache eviction even though no newer render superseded
their destination.
**Status**: Verified
**Branch**: —
**Commit**: `9015d422d97d3be0ba9aa04a0ebeeec81c934335`

## Evidence

- `am_configurator/media_composition.py:1946-1950` creates a retained preview
  session whenever the existing full-render route omits `preview_session_id`.
- `am_configurator/media_composition.py:1493-1510` applies the two-entry LRU
  limits and inserts that unreferenced session, while the render cleanup at
  `am_configurator/media_composition.py:1985-1986` only updates the active count.
- `am_configurator/media_composition.py:1850-1877` requires every render's
  prepared session to remain in that LRU, including sessionless legacy renders.
- The pre-LSR-2 renderer had no preview-session residency dependency for this
  route. The LSR-2 requirement explicitly preserves existing full-render
  behavior while adding optional prepared sessions.

The intake reproduction prepared one explicit session, issued two sessionless
renders, and then requested the explicit session's source frame. The request
failed with `This media preview session is no longer available.` even though
its Library item had not changed.

## Predicted observable failure

A user can keep media A open in the live Source/Board workflow while ordinary
full renders for media B silently consume both preview-session slots. Media A's
next selected-frame or source-frame request then fails as an expired session.
Concurrent sessionless renders can also evict one another and return HTTP 409 or
an irrelevant preview-session error despite targeting independent destinations.

## What

The new bounded cache conflates explicit reusable preview sessions with
one-request legacy rendering. A sessionless caller neither requested nor can
reuse the retained entry, but it pays the cache capacity and residency
requirements anyway.

## Approach

Decode sessionless full renders into transient prepared sources that are never
inserted into the preview LRU. Keep explicit `preview-session` sessions retained
and residency-checked. Sessionless epoch checks remain destination-scoped but
depend only on coordinator lifetime and the latest epoch, not cache membership.
Do not publish an unusable preview-session identifier for a transient render.

## Files changed

- `am_configurator/media_composition.py` — keep sessionless renders transient,
  exempt them from preview-LRU residency, and return no unusable session ID.
- `tests/test_media.py` — cache-residency and concurrent-render guards.
- `.agents/review/findings/cl-11.md` — finding and repair record.
- `.agents/review/index.md` — finding status.
- `.agents/state.md` — current LSR-2 review-loop state.

## Guard proof

`tests/test_media.py`:

- `test_sessionless_renders_do_not_consume_preview_session_lru` retains an
  explicit session across two ordinary full renders, reuses its source-frame
  endpoint afterward, and requires transient responses to expose no session ID.
- `test_sessionless_render_does_not_depend_on_preview_lru_residency` blocks one
  ordinary render, runs two independent destinations that would evict its old
  cached session, and requires all three to complete.

Proof:

- Test-first red: the retained session failed as unavailable and the blocked
  render raised `MediaRenderSuperseded`.
- Focused green: both guards passed after the repair.
- Mutation proof: restoring only the pre-repair production behavior made both
  guards fail with those exact outcomes; restoring the repair returned both
  green.
- Affected suites: 245 tests passed with 3 skips across media, app, device
  mapping, and Library coverage.
- Full gate: 687 Python tests passed with 5 skips; all 144 browser tests passed;
  Python compilation, all seven JavaScript syntax checks, and `uv build`
  completed successfully. Persisted evidence is in the machine-local
  `cl11-full-gate` artifacts.

## Coder dispute (if any)

None. The predicted failure reproduced against the reviewed commit.

## Known gaps

`cl-12` remains a separate admitted LOW finding: sessionless cancellation still
starts after decode. This repair deliberately does not claim to close it.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`4bdb75f802dec338821ea827d397a7ebc091d8bd..65c9fbfc22c2b24c3b868218512b00039756e6e1`;
claude-cli 2.1.220; capability_ok=true; verdict=findings; exit 0; no stderr;
2026-08-02T05:19:33Z.

- The schema-enforced first substantive result carried the exact pinned SHAs
  and two findings. It was persisted and used as returned without retry,
  re-emission, replacement, reformatting, or resubmission.
- This finding was independently reproduced before admission.

Accepted by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — claude-cli 2.1.220; reviewed
`9015d422d97d3be0ba9aa04a0ebeeec81c934335`; base
`bedd9b52c428fdc1cd9fb03c23a03816c7078a5c`; guard_confirmed=true;
capability_ok=true; verdict=accepted; exit 0; no stderr;
2026-08-02T05:40:48Z.

- The exact diff contained only the five declared paths.
- In a disposable worktree, the reviewer confirmed both focused guards green,
  restored only the production module from the base and reproduced exactly the
  unavailable retained session plus incorrectly superseded independent render,
  then restored the repair and returned both guards to green.
- Explicit sessions retain their LRU residency checks; sessionless epochs stay
  destination-scoped without cache residency and publish no unusable token.
- The reviewer independently passed all 687 Python tests with 5 skips, all 245
  affected tests with 3 skips, and all 144 browser tests.
- The disposable worktree was removed and pruned. The shared tree remained
  clean. Exactly one verification invocation was made; it was not retried,
  re-emitted, replaced, reformatted, or resubmitted.
