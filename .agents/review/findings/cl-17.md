# cl-17: Effect parameter changes leave timeline LED thumbnails stale

**Severity**: MEDIUM — the Board and Apply arrays update correctly, but the
shared timeline continues to show the previous effect colors indefinitely,
misrepresenting the accepted output during the slice's primary interaction.
**Status**: Verified
**Branch**: —
**Commit**: `07a1cbe8cf2c7eea56ea4aa27b43dada8a861c1a`

## Evidence

- At reviewed commit `246da643e95dbc2fc390507264e228cc75051292`,
  `am_configurator/web/app.js:4058-4063` binds the six normal effect controls
  to `regenerateLocalAnimationDraft({renderWorkspace:false})`; no trailing
  event requests a full workspace render.
- `am_configurator/web/app.js:3858-3861` handles that partial path by calling
  only `renderLightingBoardProjection()` and
  `updateLocalAnimationDraftStatus()`.
- `am_configurator/web/lighting_workspace.js:1375-1397` repaints physical
  Board pixels and frame selection metadata, but never updates a frame item's
  `.frame-thumb` colors.
- `am_configurator/web/app.js:4345` creates each thumbnail's LED swatches only
  inside `renderLightingEdit()`. No other application writer updates those
  swatches.

Direct inspection after intake reproduced the broken ownership chain: the
accepted `BoardFrameSet` changes, the partial projection uses its new active
frame for Board pixels, and the already-mounted timeline swatches retain their
old inline backgrounds.

## Predicted observable failure

Select Pulse, Hue cycle, Sweep, or Shimmer, then change Minimum brightness,
Color rotations, Sweep direction/band width, Shimmer depth, or Pattern seed.
The Board immediately shows the new effect and Apply will clone those new
arrays, but every timeline LED miniature keeps the previous parameter's colors
until an unrelated full render such as frame-count change, duration change,
tool switch, Apply, or Cancel.

## What

The focus-preserving partial repaint updates only the current physical Board
frame and timeline selection state. Timeline thumbnails are also projections
of the exact accepted frame set, but they were left owned exclusively by the
full HTML render, so normal live parameter changes split the UI between two
different outputs.

## Approach

Extend `paintBoardProjection` to repaint the mounted thumbnail swatches from
the same selected `BoardFrameSet` used by the Board. Keep the partial repaint
path so slider input does not rebuild the workspace or steal focus. The update
must address every mounted frame item, clamp to its existing swatch count, use
canonical frame colors only, and leave selection/accessibility updates intact.

## Files changed

- `am_configurator/web/lighting_workspace.js` — exact mounted-thumbnail repaint
  in the partial Board projection.
- `tests/web/lighting_workspace.test.js` — behavioral guard for changed accepted
  frames versus already-mounted swatches.
- `.agents/review/findings/cl-17.md`, `.agents/review/index.md`,
  `.agents/state.md`, and the redesign plan — repair-range records.
- `.agents/review/outcomes.md` and the same current-state records —
  post-verdict closure only; `outcomes.md` is not part of the repair diff.

## Guard proof

- Guard: `tests/web/lighting_workspace.test.js::partial Board projection
  refreshes timeline thumbnails from the accepted frame set` mounts stale
  swatches, publishes a different two-frame local-effect set, runs only
  `paintBoardProjection`, and asserts both thumbnail arrays equal the accepted
  arrays while selection state remains correct.
- Red: with the guard present and the reviewed production painter unchanged,
  22/23 workspace tests passed; only the new assertion failed, retaining
  `#AAAAAA/#BBBBBB` and `#CCCCCC/#DDDDDD` instead of the accepted colors.
- Green: after the repair, all 23 workspace tests and all 171 browser tests
  pass.
- Complete gate: 694 Python tests pass with 5 skips, all 171 browser tests
  pass, compilation and all seven JavaScript syntax checks pass, and fresh
  source and wheel archives build successfully.
- Exact repair-head CI run `30746538330` passed all four jobs and Desktop
  installers run `30746538320` passed all five platform, metadata, and
  provenance jobs.

## Coder dispute (if any)

None. The accepted frame set already owns both Board and timeline output, and
the partial painter demonstrably updates only one of them.

## Known gaps

This repair does not add a full render after parameter input, change effect
math, alter playback, expand thumbnail size, or touch media/AI/hardware paths.
The focused regression is deterministic in the browser projection seam; its
previously deferred native visual evidence passed in the final exact-`a4d3793`
LSR-10 three-platform qualification.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`6851df5624488c5aa1fa3444af096da0a020f8e9..246da643e95dbc2fc390507264e228cc75051292`;
claude-cli 2.1.220; `capability_ok=true`; verdict `findings`; exit 0; no stderr;
2026-08-02T11:42:24Z.

- The schema-enforced first substantive result carried exact pinned SHAs and
  this one MEDIUM finding. It was persisted and used as returned without
  retry, re-emission, replacement, reformatting, or resubmission.
- One hook-rewritten `rtk git` command was denied by the launch allowlist. The
  reviewer recovered through allowed repository reads and git inspection,
  independently ran all 170 browser tests, and completed capability proof.
- Immutable machine-local result SHA-256:
  `EEA7624B5F9E18FCF9FB2AACE00ECF103B6D9353D8A6D6F8E4F19B1206CD5CB6`.

Accepted by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — claude-cli 2.1.220; reviewed
`07a1cbe8cf2c7eea56ea4aa27b43dada8a861c1a`; base
`a18a0a61b68ae1f4b0cfd288e9fc7b8b9c097bae`; `guard_confirmed=true`;
`capability_ok=true`; verdict `accepted`; exit 0; no stderr;
2026-08-02T11:57:11Z.

- The first substantive result was used unchanged and was not discarded,
  retried, re-emitted, replaced, reformatted, or resubmitted.
- In a disposable detached worktree, the reviewer passed 23/23 focused and
  171/171 complete browser tests, restored only `lighting_workspace.js` from
  the base and reproduced the exact one-test stale-color failure, restored the
  reviewed file, returned both suites green, and removed the worktree without
  changing the primary checkout.
- The reviewer confirmed the exact six-path repair diff, focus-preserving DOM
  update, validated color/source parity, unchanged media selected-frame
  semantics, and absence of document, Library, provider, hardware, or network
  mutation. Its one cosmetic record note is corrected in this closure.
- Two hook-rewritten git forms, one Grep form, and one unavailable reviewer-side
  PTK output read were denied; allowed reads and git/Node commands completed
  the inspection, mutation proof, and capability proof.
- Immutable machine-local verification result SHA-256:
  `08802A29B1C9C4DA6E512DD875C8498957570EDA70BEAEF9B92F8E6A9CC2A734`.
