# n567-4: The enabled full-write path never transmits the keymap

**Severity**: HIGH — 2026-07-26 `openreview codex` over
`6c396c9..d17c2ca` (plan tasks N5, N6, N7).
**Status**: Open
**Branch**: `neon-80-support`
**Commit**: (not started)

## Evidence

am_configurator/neon_driver.py:167 - write_config preflights the configuration and line 168 sends only neon_lighting.push; vial_keymap.write_keymap is never called.

## Predicted observable failure

Lighting and then macros can change, but the old keymap remains; verification reports an accepted-write conflict and key assignments never apply.

## What

See the pass summary in `.agents/review/index.md`: the protocol modules are
sound in isolation and the seams between them and the existing application were
not built. This finding is one of those seams.

## Approach

Not started. The reviewer's suggestion: Pre-encode the complete keymap during preflight, then transmit it through the same approved session and add an integration test requiring VIA_SET_BUFFER packets and changed-key readback.

## Files changed

Not started.

## Guard proof

Not started.

## Coder dispute (if any)

None.

## Known gaps

None recorded yet.

## Reviewer comments

`Reviewer: codex / (codex-configured default)` — verdict `findings`.
