# cl-16: Interactive framing never uses the selected-frame render tier

**Severity**: MEDIUM — every small framing adjustment waits on a full
multi-frame render, making long GIF editing serial and visibly stale despite
the approved fast-preview contract and existing backend endpoint.
**Status**: In progress
**Branch**: —
**Commit**: pending

## Evidence

- The reviewed browser tree has no caller for
  `POST /api/library/items/<catalog-id>/render-frame`; the only application
  occurrence is the server route.
- `am_configurator/web/app.js:198-204` creates one scheduler whose runner is
  `renderMediaCompositionPreviewAttempt`.
- `am_configurator/web/app.js:3292` posts only to the full `/render`
  endpoint.
- Transform, effect, preset, and destination changes all reach that same full
  render scheduler through `requestMediaCompositionRender`.
- `am_configurator/web/lighting_workspace.js:807` supports
  `FRAME_RENDER_STARTED`, but this range never dispatches it.
- The approved LSR-5 plan explicitly requires exact selected-frame requests
  with one in-flight and one latest pending request, followed by a short idle
  debounce for the full sequence that alone enables Apply.

TokenSave and direct pinned-tree search after intake confirmed the
`render_frame` backend method exists while no browser caller exists.

## Predicted observable failure

On a long GIF, every drag, keyboard nudge, preset, or destination adjustment
leaves Board showing the previous accepted LEDs until the whole sequence
finishes. Two quick adjustments serialize behind two full renders, and Apply
stays disabled throughout. Latency scales with total frame count instead of
the selected frame.

## What

LSR-5 implemented debouncing and latest-only execution for the full sequence
but omitted the separate immediate selected-frame tier that the plan and
backend contract reserve for interactive truthfulness.

## Approach

Add a selected-frame scheduler with one running request and one latest pending
request. Interactive transform/effect changes immediately render the current
playhead through `/render-frame` and publish only that exact Board frame.
After the latest selected-frame result is accepted, start or restart the short
bounded idle debounce for the existing full-sequence render. Only the exact
accepted full sequence may satisfy Apply; selected-frame publication never
launders partial work into the document. Preserve Source/Board timeline
identity, stale context rejection, preview-session recovery, and cancellation
across both tiers.

## Files changed

- `am_configurator/web/app.js` — frozen immediate selected-frame scheduler,
  strict caller, session recovery, and full-sequence handoff.
- `am_configurator/web/lighting_workspace.js` — frozen transient selected-frame
  adapter and reducer/selector ownership without full-sequence promotion.
- `am_configurator/web/library_state.js` — frozen canonical selected-frame
  transform/effect acceptance that cannot satisfy Apply.
- `am_configurator/media_framing_audit.py` — frozen native delayed-full-render
  proof for latest-only selected-frame output and eventual Apply equality.
- `tests/web/lighting_shell.test.js`, `tests/web/lighting_workspace.test.js`,
  `tests/web/library_state.test.js`, and `tests/test_media_framing_audit.py` —
  focused guards.
- `.agents/review/findings/cl-16.md`, `.agents/review/index.md`,
  `.agents/review/outcomes.md`, `.agents/state.md`, and the redesign plan —
  normal implementation/review records.

## Guard proof

Required behavioral proof must delay the full sequence, make several
interactive revisions, and observe the exact latest selected-frame Board
result before the full render is released. It must also prove requests never
overlap, stale selected-frame responses cannot enable Apply, and the full
accepted arrays still exactly match the eventual Apply payload. Removing the
`render-frame` caller must fail that proof.

Proof:

- Focused green: all 77 shell/composer/workspace/Library browser tests and all
  15 media-framing audit contract tests pass.
- Mutation red: replacing only the selected-frame scheduler request with the
  full-sequence scheduler makes the exact shell guard fail. The same mutation
  makes the native audit exit 1 with `mapped_frames_mismatch`, before it can
  reach the selected-frame tier, because Preview receives the wrong scheduler
  result. Machine-local evidence is
  `cl16-native-audit-mutation-20260802-062343.local.json`.
- Native green: after restoring the selected-frame scheduler, GIF, PNG, and BMP
  pass at 1000×680 and 1280×800 with `selected_frame_tier`, exact eventual
  full-sequence equality, zero console errors, and zero layout findings.
  Machine-local evidence is
  `cl16-native-audit-green-20260802-062418.local.json`; no mutation marker
  remains.
- Full gate: the guarded CI-equivalent command chain advanced through Python
  tests, compilation, all browser tests, all seven JavaScript syntax checks,
  and `uv build`, producing fresh source and wheel archives at
  2026-08-02T10:26:29Z. PTK lost its outer transport immediately after those
  archives were created, so the original evidence is retained and no duplicate
  full gate was submitted.

## Coder dispute (if any)

None. The approved plan names this tier, the server already exposes it, and the
reviewed browser has no caller.

## Known gaps

This repair is the largest of the three findings and must land only after
`cl-14` preserves ownership across tool transitions. It must not broaden
format support, alter export compatibility, or change hardware-write behavior.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`e97b40280a494ff5446fb2954fe01ed84f565924..7052212445c269752a094217b1ab4813741b2ef7`;
claude-cli 2.1.220; capability_ok=true; verdict=findings; exit 0; no stderr;
2026-08-02T09:18:12Z.

- The schema-enforced first substantive result carried exact pinned SHAs and
  three findings. It was persisted and used as returned without retry,
  re-emission, replacement, reformatting, or resubmission.
- The environment recorded the same two non-terminal permission denials noted
  in `cl-14`; capability proof still completed.
