# n567-1: Neon device reads cannot produce a valid API response

**Severity**: HIGH — 2026-07-26 `openreview codex` over
`6c396c9..d17c2ca` (plan tasks N5, N6, N7).
**Status**: Open
**Branch**: `neon-80-support`
**Commit**: (not started)

## Evidence

am_configurator/server.py:2039 - /api/device/read serializes HidDeviceInfo with asdict; its bytes path is not JSON-serializable, while derived product_id is omitted.

## Predicted observable failure

Clicking Read keymap & macros for a Neon finishes the HID reads but returns a generic HTTP 500, so the UI can never load the device workspace.

## What

See the pass summary in `.agents/review/index.md`: the protocol modules are
sound in isolation and the seams between them and the existing application were
not built. This finding is one of those seams.

## Approach

Not started. The reviewer's suggestion: Use transport.device_json(handle, device) for every device response and add an endpoint test using a real HidDeviceInfo-shaped value.

## Files changed

Not started.

## Guard proof

Not started.

## Coder dispute (if any)

None.

## Known gaps

None recorded yet.

## Reviewer comments

`Reviewer: codex / (codex-configured default)` — verdict `findings`.
