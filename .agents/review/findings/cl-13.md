# cl-13: Expired media preview sessions cannot recover

**Severity**: HIGH — an ordinary five-minute idle period or bounded-LRU
eviction permanently breaks Preview and Source for the open media draft until
the user cancels and discards it or imports the file again.
**Status**: Verified
**Branch**: —
**Commit**: `abc6826b346420de257d1679879ef84e483c3a81`

## Evidence

- `am_configurator/web/app.js:2718-2721` returns any cached
  `preview_session_id` without revalidating it.
- `am_configurator/web/app.js:3046-3055` and `am_configurator/web/app.js:2808-2816`
  reuse that ID for render and source-frame requests; neither failure path
  clears it.
- `am_configurator/web/lighting_workspace.js:704-719` can set a preview session
  but has no event that invalidates only the expired session while preserving
  the draft.
- `am_configurator/media_composition.py:34,1537-1549` deliberately expires
  sessions after five minutes and evicts them from a two-entry LRU, returning
  `This media preview session is no longer available.` for the stale ID.

The browser/server contract was independently checked against the reviewed
commit after intake: the cached ID survives render failure, while every retry
short-circuits before creating a replacement session.

## Predicted observable failure

Import media and create a preview, then either leave it idle for more than five
minutes or let two newer preview sessions evict it. Preview sends the expired
ID and fails. Every later Preview sends the same ID and fails again; Source
frame loading also remains unavailable. Cancel or re-import is the only current
recovery, which discards the user's active framing workflow.

## What

LSR-4 introduced retained preview sessions so Source can request exact decoded
frames. The frontend treats the retained ID as permanent even though the server
contract intentionally makes it short-lived.

## Approach

Add a reducer-owned, identity-checked session-invalidation event that clears
only the expired ID and preserves the media draft and accepted Board result.
Have render detect the server's exact unavailable-session response, invalidate
the request's ID, and retry once through normal session creation. Source-frame
failure invalidates the same ID so the next Preview can recover. Extend the
native audit to evict the active session and prove replacement plus successful
render in the real WebView2 integration path.

## Files changed

- `am_configurator/web/lighting_workspace.js` — identity-checked session invalidation.
- `am_configurator/web/app.js` — exact expiry detection and one bounded render retry.
- `am_configurator/media_framing_audit.py` — native eviction/recovery proof.
- `tests/web/lighting_workspace.test.js` — reducer replacement and stale-event guards.
- `tests/web/lighting_shell.test.js` — app recovery wiring contract.
- `tests/test_media_framing_audit.py` — native proof contract.
- `.agents/review/findings/cl-13.md` — finding and repair record.
- `.agents/review/index.md` — finding status.
- `.agents/state.md` — current review-loop pointer.

## Guard proof

`tests/web/lighting_workspace.test.js` —
`expired media sessions clear only the matching id and preserve the draft`
requires exact-ID invalidation to preserve the media payload and accepted
preview, accept a replacement ID, and reject a late invalidation of that newer
session.

`am_configurator/media_framing_audit.py` — the native WebView2 audit creates two
new sessions to evict the active ID, observes the old render fail, clicks
Preview, and requires a different session ID, ready render, renewed actual
Source frame, unchanged document, and unchanged Undo stack for GIF, PNG, and
BMP at 1000×680 and 1280×800.

Proof:

- Test-first red: the new browser tests failed because the reducer rejected
  `MEDIA_SESSION_INVALIDATED` and the app had no bounded recovery path; the
  native-audit contract test failed because no recovery check existed.
- Focused green: 50 shell/workspace browser tests and the native-audit contract
  test passed after the repair.
- Native green: all six format/viewport cases recovered from deliberate LRU
  eviction with zero console errors and zero layout findings.
- Mutation proof: replacing only the render retry predicate with `false` made
  the native audit fail at `preview_session_recovery_timeout`; restoring the
  predicate returned all six cases green. No mutation marker remains.
- Affected suites: all 153 browser tests and 32 media/audit Python tests passed.
- Full gate: 691 Python tests passed with 5 skips; all 153 browser tests passed;
  Python compilation, all seven JavaScript syntax checks, and `uv build`
  completed successfully. Machine-local evidence uses the
  `cl13-full-gate-20260802-034318` and `cl13-*-native-*` stems.

## Coder dispute (if any)

None. The bounded server lifetime and permanent client cache are directly
observable at the reviewed commit.

## Known gaps

LSR-5 still owns request coalescing, source-frame prefetch, and holding playback
when a projection is late. This repair only restores bounded recovery from the
server's existing session lifetime contract.

## Reviewer comments

Raised by: Reviewer: claude / claude-opus-5 / high / standard (inline,
session-only; job `fable-review`) — generation pass over
`b0dee7ef725771ad43d7d60d3b8f96aa83e3df1b..78bcdcf47ff3a5dcacce555ad31ac14bef95993b`;
claude-cli 2.1.220; capability_ok=true; verdict=findings; exit 0; no stderr;
2026-08-02T07:36:03Z.

- The schema-enforced first substantive result carried the exact pinned SHAs
  and this one finding. It was persisted and used as returned without retry,
  re-emission, replacement, reformatting, or resubmission.
- The launch recorded three permission denials, including the known hook rewrite
  of one `git` command to `rtk`; the reviewer recovered with allowed read and git
  operations and returned `capability_ok=true`. Per the playbook, the environment
  quirk is recorded and does not invalidate the substantive result.
- The finding was independently reproduced at intake from the explicit client
  cache and server expiry paths.

Accepted by: Reviewer: claude / claude-opus-5 / xhigh / frontier escalated: T2
(inline, session-only; job `fable-review`) — claude-cli 2.1.220; reviewed
`abc6826b346420de257d1679879ef84e483c3a81`; base
`78bcdcf47ff3a5dcacce555ad31ac14bef95993b`; guard_confirmed=true;
capability_ok=true; verdict=accepted; exit 0; no stderr;
2026-08-02T07:56:47Z.

- Scope check: the 78bcdcf..abc6826 diff touches exactly the nine paths declared in .agents/review/findings/cl-13.md (3 source/audit files, 3 test files, 3 record files); no undeclared path and no unrelated behavior change. The code files sit inside the LSR-4/LSR-5 file boundaries in docs/superpowers/plans/2026-08-01-lighting-studio-human-first-redesign.md, and LSR-5's still-open items (coalescing, source prefetch, late-projection playback hold) are untouched and recorded as known gaps.
- Repair closes cl-13. lighting_workspace.js:720-733 adds MEDIA_SESSION_INVALIDATED, which returns unchanged(state,'stale') unless catalog_id, asset_id, and preview_session_id all match the current media, then removes only preview_session_id and emits a single render-workspace intent. app.js:3062-3146 splits render into renderMediaCompositionPreview -> Attempt(true), invalidates the exact requested session on the server's unavailable-session error, and returns Attempt(false), so recovery is bounded to one retry through ordinary ensureMediaPreviewSession. app.js:2854-2857 clears the same dead ID from the source-frame failure path so the next Preview reconnects.
- Draft and Board preservation verified in code and test: only the preview_session_id key is dropped; media payload, state.preview, context_epoch, and playhead are untouched, and no playback cancel intent is emitted. The workspace test asserts requested_transform survival and preview identity equality (state.preview === beforePreview).
- Stale-session isolation is doubly guarded. Caller side requires workspaceAsyncContextMatches plus unchanged preview.request_epoch and request_context_key (app.js:3134-3140); reducer side independently rejects an invalidation naming an older session while a newer one is installed, and the test proves the newer 'b'*32 id survives a late 'a'*32 invalidation with ignored='stale' and state identity preserved.
- Verification at reviewed head in a disposable detached worktree (F:\dev\_amkb-review\cl13-abc6826, git worktree add --detach abc6826): node --test tests/web/lighting_workspace.test.js tests/web/lighting_shell.test.js -> 50 pass / 0 fail, exit 0, including 'expired media sessions clear only the matching id and preserve the draft'. uv run --frozen --extra desktop python -m unittest discover -s tests -p test_media_framing_audit.py -v -> 14 tests OK, exit 0.
- Native eviction path at reviewed head: uv run --frozen --extra desktop python -m am_configurator.desktop --media-framing-audit <worktree>/cl13-head-native.local.json -> exit 0, status=passed, failure=null, 2 viewports x 3 formats. preview_session_recovery present for gif, png, and bmp at both 1000x680 and 1280x800; console_errors=0 and layout_findings=0 in both viewports. Server log shows the intended shape: two extra preview-session creates, a 400 render on the evicted id, then a fresh 201 preview-session and successful render.
- Mutation proof (non-vacuous): replacing only am_configurator/web/app.js with the base 78bcdcf version via git checkout inside the disposable worktree, leaving the reviewed head's tests and audit in place, made the native audit exit 1 with {"failure":"preview_session_recovery_timeout","status":"failed","viewports":[]} - exactly the predicted reason. Restoring app.js from abc6826 and rerunning returned exit 0, status=passed, all six format/viewport cases green with preview_session_recovery, 0 console errors, 0 layout findings.
- Adjacent surface clean: full node --test tests/web/*.test.js -> 153/153 pass; uv run --frozen --extra desktop python -m unittest discover -s tests -p test_media*.py -> 32 tests OK; node --check clean on am_configurator/web/app.js and am_configurator/web/lighting_workspace.js. No regression observed in the touched surface.
- Primary working tree was never mutated: all edits and audits ran in the detached worktree, which was removed with git worktree remove --force. After cleanup F:\dev\AMKB-GUI is still at abc6826 with zero tracked modifications and a single worktree entry.
- Non-material observation (no action required): recovery keys on the exact English server string 'This media preview session is no longer available.', which media_composition.py raises from six sites, rather than a stable error code - a message edit or localization would silently disable recovery, though the bounded single retry keeps the failure mode safe.
- Non-material observation (no action required): the source-frame failure path does Object.assign(error, failure, {status}), so a server payload carrying a 'message' field would override the constructed message; current error payloads carry only 'error'.
- Capability proof: read AGENTS.md, .agents/repo-guidance.md, .agents/review/findings/cl-13.md, the LSR-4/LSR-5 plan sections, and repository source files (app.js, lighting_workspace.js, server.py) before ruling; ran the allowlisted git, node, and uv commands listed above. Pins match - worktree HEAD resolved to abc6826b346420de257d1679879ef84e483c3a81 and the mutation file was sourced from 78bcdcf47ff3a5dcacce555ad31ac14bef95993b.

The first substantive schema result was persisted and used as returned. It was
not retried, discarded, replaced, reformatted, or resubmitted. Its complete
machine-local envelope has SHA-256
`B87DA2B642FFBBC989B2972E63BD3887FEF48C2773E2C3B131A021C03493AC8B`.
